##if tolertation are not give if schedular will allow the pod to tainted node?

apiVersion: v1
kind: Pod
metadata:
  name: tolerations1
  labels:
    purpose: "toleration-demo"
spec:
  nodeSelector: 
    topology.kubernetes.io/zone: us-east-1d
  containers:
  - name: nginx
    image: nginx